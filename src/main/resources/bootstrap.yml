spring:
  application:
    name: products
  config:
    import: 
      - file:.env[.properties]
      - "optional:configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST}/eureka/

